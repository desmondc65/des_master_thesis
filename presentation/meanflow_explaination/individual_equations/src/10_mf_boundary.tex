\documentclass[border=10pt,varwidth=\maxdimen]{standalone}
\usepackage{amsmath,amssymb,bm}
\usepackage{xcolor}

\begin{document}
\(\displaystyle \lim_{r \to t} u(z_r, r, t) \;=\; u(z_t, t, t) \;=\; v \qquad (\text{the diagonal } r=t \text{ is exactly flow matching}) \)
\end{document}
