% MeanFlow StormCast -- training step workflow (horizontal, matches the
% inference figure). Highlights the Phase-2 block MeanFlow swaps in for
% StormCast's multi-step EDM diffusion (cf. StormCast paper, Fig. 1b).
% Notation: thesis formula deck (X_{t-1} state, M_t = F_xi(...) mean,
% R_t = X_t - M_t residual, c_t = concat(X_{t-1},M_t,I)).
\documentclass[border=14pt]{standalone}
\input{mf_style}
\begin{document}
\begin{tikzpicture}[node distance=10mm]

  % ============================ inputs ============================
  \node[data, text width=2.5cm] (xprev) at (0,1.5)  {$X_{t-1}$\\[1pt]\scriptsize HighRes state · 4\,ch};
  \node[data, text width=2.5cm] (st)    at (0,0)    {$S_t$\\[1pt]\scriptsize ERA5 background · 24\,ch};
  \node[data, text width=2.5cm] (inv)   at (0,-1.5) {$I$\\[1pt]\scriptsize invariants (lsm, orog)};

  % ===================== Phase 1: frozen regression (shared with StormCast) =====================
  \node[frozen, text width=2.7cm] (reg) at (3.6,0)  {\textbf{regression $F_\xi$}\\[2pt]\scriptsize StormCastUNet\\frozen};
  \node[frozen, text width=2.6cm] (mt)  at (7.0,0)  {$M_t$\\[1pt]\scriptsize regression mean · 4\,ch};

  % ----- conditioning branch (concat) -----
  \node[concat] (cc) at (8.7,1.5) {};
  \node[data, text width=3.1cm] (cond) at (11.2,1.5) {\textbf{condition $c_t$}\\[2pt]\scriptsize $\mathrm{concat}(X_{t-1},M_t,I)$};

  % ----- residual branch (subtract) -----
  \node[data, text width=2.5cm] (xt) at (7.0,-3.2) {$X_t$\\[1pt]\scriptsize target HighRes · 4\,ch};
  \node[addop] (sub) at (8.7,-1.6) {$-$};
  \node[data, text width=2.9cm] (resid) at (11.2,-1.6) {$R_t = X_t - M_t$\\[1pt]\scriptsize residual target};

  % ===================== Phase 2: MeanFlow objective (the swapped-in block) =====================
  \node[sampler, text width=4.3cm] (flow) at (15.6,-1.0)
    {\textbf{flow-space setup}\\[2pt]\scriptsize $x_1{=}R_t/\sigma_{\mathrm{data}}$,\; $x_0{\sim}\mathcal N(0,I)$\\[1pt]$v{=}x_1{-}x_0$,\; $z_r{=}(1{-}r)x_0{+}r\,x_1$\\[1pt]sample $r\le t$};
  \node[trained, text width=3.5cm] (uth) at (21.0,0.7)
    {\textbf{$u_\theta(z_r,r,t,c_t)$}\\[2pt]\scriptsize average velocity\\single forward · two times $(r,t)$};
  \node[trained, text width=4.3cm] (tgt) at (21.0,-2.0)
    {\textbf{matching target}\\[2pt]\scriptsize MeanFlow id.\ ($r{<}t$, 25\%):\\$u_{\mathrm{tgt}}{=}v{+}(t{-}r)\,\mathrm{d}u/\mathrm{d}r$ \,(JVP, sg)\\[1pt]I-CFM ($t{=}r$, 75\%):\; $u_{\mathrm{tgt}}{=}v$};

  \begin{scope}[on background layer]
    \node[newbox, fit=(flow)(uth)(tgt)] (p2) {};
  \end{scope}

  % ===================== loss + optimizer/EMA =====================
  \node[loss, text width=4.1cm] (lossn) at (26.4,-0.6)
    {\textbf{loss}\\[2pt]\scriptsize $\mathcal L=\mathcal L_{\mathrm{MF}}+\alpha_{\mathrm{spec}}\mathcal L_{\mathrm{spec}}$\\[1pt]adaptive velocity matching,\\$\beta{=}(1,1,1,2)$ $+$ qpepre log-PSD};
  \node[output, text width=4.0cm] (opt) at (31.3,-0.6)
    {\textbf{AdamW $+$ EMA}\\[2pt]\scriptsize lr $5\!\times\!10^{-4}$ · cosine · clip $1$\\[1pt]$\bar\theta\leftarrow0.999\,\bar\theta$ \;$\to$\; \texttt{ema\_state.pt}};

  % ===================== ghost: the StormCast block being replaced =====================
  \node[ghost, text width=5.2cm] (ghost) at (16.8,3.5)
    {\textbf{StormCast --- Phase 2 (replaced)}\\[2pt]\scriptsize EDM diffusion $D_\phi$ · denoising score-matching\\[1pt]$\sigma\sim$ Karras · sampled $\approx$\,35 NFE (18 Heun)};
  \draw[replaced] (ghost.south) to[out=-90,in=110] (uth.north);
  \node[lg, text=cNew, anchor=west] at ($(ghost.east)+(2mm,-7mm)$) {swapped for 1--2 NFE};

  % badge (tab on the container's top edge) + descriptive label on the new block
  \node[badge, anchor=south east] at ($(p2.north east)+(-3mm,0.3mm)$) {NEW vs StormCast};
  \node[sub, text=cNew, anchor=north] at ($(p2.south)+(0,-2mm)$)
    {Phase 2 --- average-velocity flow matching \;(replaces StormCast EDM diffusion $D_\phi$)};

  % ===================== edges =====================
  % phase 1
  \draw[flow] (xprev.east) -- (reg.west);
  \draw[flow] (st.east) -- (reg.west);
  \draw[flow] (inv.east) -- (reg.west);
  \draw[flow] (reg.east) -- (mt.west);
  % conditioning (concat): X_{t-1}, M_t (+ I, see label) -> cc -> cond
  \draw[flow] (mt.north) to[out=90,in=-90] (cc.south);
  \draw[thin flow] (xprev.east) -- (cc.west);
  \draw[flow] (cc.east) -- (cond.west);
  % residual (subtract): X_t - M_t -> sub -> resid
  \draw[flow] (mt.south) to[out=-90,in=120] (sub.north);
  \draw[flow] (xt.east) to[out=0,in=-120] (sub.south);
  \draw[flow] (sub.east) -- (resid.west);
  % phase 2
  \draw[flow] (resid.east) -- (flow.west);
  \draw[flow] (flow.east) to[out=25,in=200] node[pos=0.5,above,lg]{$z_r,r,t$} (uth.south west);
  \draw[flow] (flow.east) to[out=-20,in=180] node[pos=0.5,below,lg]{$v$} (tgt.west);
  \draw[flow] (cond.east) to[out=0,in=155] node[pos=0.55,above,lg]{$c_t$} (uth.west);
  % loss
  \draw[flow] (uth.east) to[out=0,in=150] node[pos=0.5,above,lg]{$u_\theta$} (lossn.north west);
  \draw[flow] (tgt.east) to[out=0,in=210] node[pos=0.5,below,lg]{$u_{\mathrm{tgt}}$} (lossn.south west);
  \draw[flow, line width=1.0pt, draw=black!70] (lossn.east) -- (opt.west)
        node[midway,above,lg]{backward};

  % ===================== title + subtitle (above the ghost) =====================
  \node[ttl, anchor=south west] (title) at ($(xprev.north west)+(0,32mm)$)
    {MeanFlow StormCast --- training step};
  \node[sub, anchor=north west] at ($(title.south west)+(0,-0.5mm)$)
    {average-velocity flow matching on the frozen-regression residual \;·\; one network forward per micro-batch};

  % ===================== legend =====================
  \node[lg, anchor=north west] at ($(xt.south west)+(0,-7mm)$)
    {\swatch{cData}{data / tensors}\quad
     \swatch{cFrozen}{frozen (shared w/ StormCast)}\quad
     \swatch{cSamp}{flow-space}\quad
     \swatch{cFlow}{trained MeanFlow net}\quad
     \swatch{cLoss}{loss}\quad
     \swatch{cOut}{optimizer / EMA}};
  \node[lg, anchor=north west] at ($(xt.south west)+(0,-12mm)$)
    {\tikz[baseline=-0.5ex]\node[rounded corners=1.5pt, draw=cNew, fill=cNewF, line width=1.1pt, minimum width=3.4mm, minimum height=2.6mm, inner sep=0]{};\;new vs StormCast\qquad
     \tikz[baseline=-0.5ex]\node[concat, minimum size=2.8mm]{};\;concatenate\qquad
     \tikz[baseline=-0.5ex]\node[addop, minimum size=3.6mm]{\tiny$-$};\;subtract\qquad
     \tikz[baseline=-0.5ex]\node[ghost, minimum width=4mm, minimum height=2.6mm, inner sep=0]{};\;StormCast block replaced by MeanFlow};

\end{tikzpicture}
\end{document}
